{
  "name": "datahogo-monorepo",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @datahogo/core && npm run build -w datahogo && npm run build -w @datahogo/mcp",
    "test": "npm run test --workspaces"
  }
}
